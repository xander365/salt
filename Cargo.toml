[package]
name = "payroll"
version = "0.1.0"
edition = "2024"

[dependencies]
chrono = { version = "0.4", default-features = false, features = ["alloc"] }
rust_decimal = "1"

[dev-dependencies]
rust_decimal_macros = "1"
